{
  "database_path": {
    "description": "SQLite 数据库文件路径",
    "type": "string",
    "hint": "留空则默认存放在 AstrBot 数据目录；也可以填绝对路径，插件首次启动会自动建表。",
    "obvious_hint": true,
    "default": ""
  },
  "currency": {
    "description": "货币显示名",
    "type": "string",
    "default": "馕"
  },
  "admins": {
    "description": "管理员用户 ID 列表",
    "type": "list",
    "hint": "填写平台用户 ID，例如 QQ 号。管理员可操作他人账户和 admin.* 指令。",
    "default": []
  },
  "redeem_code_on_register": {
    "description": "注册后自动发放的兑换码",
    "type": "string",
    "hint": "填已有兑换码（例如 ABC123），新用户注册时自动领取；留空则注册时不送礼。",
    "default": ""
  },
  "points_per_amount": {
    "description": "封顶金额积分折算（元/积分）",
    "type": "int",
    "hint": "默认 10：触发封顶时按封顶金额折算，每 10 元封顶得 1 积分（向上取整，如白天封顶 69 得 7、会员夜晚封顶 59 得 6、24h 封顶 99 得 10）；正常时段按游玩小时数 1 小时 1 积分（不足 1 小时不计）；设 0 关闭积分。",
    "default": 10
  },
  "max_active_checkcodes": {
    "description": "同时存在的最大验证码数量（店内人数上限）",
    "type": "int",
    "hint": "默认 20。每个场内玩家对应一个唯一验证码；当前在线人数超过该值时，入场成功后会追加提示。",
    "default": 20
  },
  "self_open_door_enabled": {
    "description": "自助开门开关",
    "type": "bool",
    "hint": "默认开启。开启后：积分高于所设门槛的用户入场时生成唯一 7 位验证码，可发送『开门+验证码』自助开门；积分不满足时仍可正常 /login 入场，但不生成验证码，并提示联系管理员开门。关闭后：所有用户入场时均不生成验证码，任何开门请求也会回复请联系管理员。",
    "default": true
  },
  "self_open_door_points_threshold": {
    "description": "自助开门积分门槛（需高于）",
    "type": "int",
    "hint": "仅在自助开门开关开启时生效，默认 10。用户可用积分必须严格高于该值，入场时才会生成验证码并允许使用 /开门；例如设为 10 时，积分为 10 不生成验证码，积分为 11 才生成。积分不足不影响 /login 入场。",
    "default": 10
  },
  "login_grace_minutes": {
    "description": "成功自助开门后的首小时免费时间（分钟）",
    "type": "int",
    "hint": "默认 3 分钟，仅对本次入场已成功使用 /开门 的玩家生效，并从入场时间开始计算。入场后不超过该时间离场 → 0 费用、不扣余额、不发积分；严格超过该时间但仍在第一个小时内 → 直接按 1 小时结算。未成功开门的玩家以及首小时之后的零头，继续使用 billing.grace_minutes（默认 15 分钟）。",
    "default": 3
  },
  "sneak_login_enabled": {
    "description": "偷偷上机功能开关",
    "type": "bool",
    "hint": "开启后仅已注册且积分高于所设门槛的用户可发送 /偷偷上机 偷偷入场（未注册用户回复「用户未注册」，积分条件不满足时禁止入场），其余计费流程与 /login 一致；会话会标记为「偷偷上机」，同时所有展示该玩家 QQ 昵称或 QQ 号的位置一律显示为「未知玩家」，自动撤回玩家发送的偷偷上机指令及其退场指令消息（仅 aiocqhttp 平台生效，撤回失败不影响入场/退场）。",
    "default": false
  },
  "sneak_login_points_threshold": {
    "description": "偷偷上机积分门槛（需高于）",
    "type": "int",
    "hint": "默认 10。用户可用积分必须严格高于该值才能偷偷上机；例如设为 10 时，积分为 10 不满足条件，积分为 11 才允许。",
    "default": 10
  },
  "billing": {
    "description": "计费规则与价格",
    "type": "object",
    "items": {
      "day_start": {
        "description": "白天开始时间",
        "type": "string",
        "default": "11:30"
      },
      "day_end": {
        "description": "白天结束时间",
        "type": "string",
        "default": "00:00"
      },
      "day_price": {
        "description": "白天每小时价格（元）",
        "type": "int",
        "default": 12
      },
      "day_price_pass": {
        "description": "白天月卡用户每小时价格（元）",
        "type": "int",
        "default": 11
      },
      "day_cap": {
        "description": "白天封顶价格（元）",
        "type": "int",
        "default": 69
      },
      "day_cap_pass": {
        "description": "白天月卡用户封顶价格（元）",
        "type": "int",
        "default": 59
      },
      "night_start": {
        "description": "夜晚开始时间",
        "type": "string",
        "default": "00:00"
      },
      "night_end": {
        "description": "夜晚结束时间",
        "type": "string",
        "hint": "默认 12:00。已经进入包夜计费的会话可持续到该时间；与白天开始时间重叠时，11:30 后新入场用户仍优先按白天计费。",
        "default": "12:00"
      },
      "late_day_start": {
        "description": "深夜入场首小时按白天计费的开始时间",
        "type": "string",
        "hint": "默认 23:00。该时间至 day_end 之间入场，首小时完整按白天价格计费，即使跨过午夜。",
        "default": "23:00"
      },
      "night_cap_cover_start": {
        "description": "首小时费用纳入包夜封顶的开始时间",
        "type": "string",
        "hint": "默认 23:30。该时间至 day_end 之间入场，首小时白天费用会与后续夜间费用合并参与包夜封顶；合计达到封顶后只收包夜封顶，不再额外叠加首小时费用。包夜结束后的白天费用仍另行计算。",
        "default": "23:30"
      },
      "night_price": {
        "description": "夜晚每小时价格（元）",
        "type": "int",
        "default": 13
      },
      "night_price_pass": {
        "description": "夜晚月卡用户每小时价格（元）",
        "type": "int",
        "default": 12
      },
      "night_cap": {
        "description": "夜晚封顶价格（元）",
        "type": "int",
        "default": 69
      },
      "night_cap_pass": {
        "description": "夜晚月卡用户封顶价格（元）",
        "type": "int",
        "default": 59
      },
      "cap_24h": {
        "description": "连续 24 小时封顶价格（元）",
        "type": "int",
        "default": 99
      },
      "cap_24h_pass": {
        "description": "连续 24 小时月卡用户封顶价格（元）",
        "type": "int",
        "default": 88
      },
      "grace_minutes": {
        "description": "宽限分钟：不足该分钟数的零头小时不计费",
        "type": "int",
        "default": 15
      }
    }
  }
}
