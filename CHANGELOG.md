# 更新日志

## 未发布

### 修复

- 为会话增加明确关闭原因，区分正常结算、普通宽限取消、开门宽限取消和管理员强制退场。
- 普通历史记录默认隐藏宽限期内取消的会话，避免大量 0 元记录挤占展示；管理员历史仍保留完整审计记录。
- 数据库升级时自动增加关闭原因字段和历史查询索引，既有会话不删除、不重分类。
- 管理员「死给」指令除 `@用户` 外，支持直接输入 QQ 号指定强制退场目标。

## v0.4.0 - 2026-08-25

### 重构

- 将原有巨型业务类拆分为用户、会话、资产、钱包、礼包兑换和计费结算协作者。
- 将金额与异常、配置解析、纯计费引擎、数据库 Schema、存储和迁移拆成独立基础模块。
- 将 AstrBot 事件解析与 OneBot API 调用隔离到平台适配层，展示层不再依赖业务门面。
- 将 `ShinjukuService` 调整为兼容门面，统一管理数据库连接和事务。

### 兼容性

- 保留原有 `ShinjukuService` 方法、金额函数、异常与常量导出。
- 保持现有指令、配置项和 SQLite 数据兼容。
- 升级时继续执行已有的数据库自动迁移流程。
- 各业务协作者复用调用方连接和事务，不自行提交事务。

### 验证

- 自动化测试覆盖 75 项场景。
- 覆盖计费规则、金额整数分、数据库迁移、身份约束、并发兑换和事务回滚。
- `python -m compileall -q .` 与 `git diff --check` 通过。
